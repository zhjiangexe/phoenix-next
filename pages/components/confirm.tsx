import styled from "styled-components"

const ConfirmLightBox = styled.div`
  position: absolute;
`

export default function Confirm({title, description, confirm, cancel}: any) {
  return <div>


  </div>
}